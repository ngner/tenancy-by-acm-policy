---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: policies
resources:
  - placement-hub.yaml
  - placement-managed-by-capability.yaml
  - placement-managed-vm-capability.yaml
  # Managed-cluster placement — uncomment ONE base strategy instead of capability default:
  # - placement-managed.yaml                         # All non-hub clusters (legacy)
  # - placement-managed-by-clusterset.yaml           # ManagedClusterSet membership
  # - placement-managed-by-label.yaml                # Opt-in tenant-eligible=true

commonAnnotations:
  argocd.argoproj.io/compare-options: IgnoreExtraneous
