---
apiVersion: policy.open-cluster-management.io/v1
kind: PolicyGenerator
metadata:
  name: policy-hub-tenancy-access-control
placementBindingDefaults:
  name: binding-hub-tenancy-access-control
policyDefaults:
  categories:
    - AC Access Control
  controls:
    - AC-3 Access Enforcement
  namespace: policies
  policySets:
    - tenancy-hub-access-control
  remediationAction: enforce
  severity: high
  standards:
    - NIST SP 800-53
  complianceType: mustonlyhave
  consolidateManifests: false

policies:
  # =========================================================================
  # All tenants — hub RBAC driven by Tenant CRs in the tenancies namespace.
  # To add a tenant, create a Tenant CR. The object-templates-raw manifests
  # iterate all Tenant CRs at evaluation time and generate:
  #   - bridge ConfigMap (consumed by managed-cluster policies)
  #   - ClusterRoleBindings for ACM console access
  #   - MulticlusterRoleAssignments for managed-cluster VM RBAC
  # =========================================================================
  - name: policy-tenant-crd-create
    manifests:
    - path: tenancy/tenant-crd.yaml
  - name: policy-tenant-bridge
    namespace: tenancies
    manifests:
    - path: tenancy/bridge-tenants.yaml
    dependencies:
      - name: policy-tenant-crd-create

policySets:
  - name: tenancy-hub-configuration-management
    description: Hub-side tenant configuration management
    placement:
      placementName: placement-hub-clusters