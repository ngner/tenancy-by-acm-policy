---
apiVersion: policy.open-cluster-management.io/v1
kind: PolicyGenerator
metadata:
  name: policy-managed-tenancy-access-control
placementBindingDefaults:
  name: binding-managed-tenancy-access-control
policyDefaults:
  categories:
    - AC Access Control
  controls:
    - AC-3 Access Enforcement
  namespace: policies
  policySets:
    - tenancy-managed-access-control
  remediationAction: enforce
  severity: high
  standards:
    - NIST SP 800-53
  complianceType: mustonlyhave
  consolidateManifests: false

policies:
  # =========================================================================
  # All tenants — managed cluster RoleBindings driven by the bridge ConfigMap.
  # To add a tenant, create a Tenant CR in the tenancies namespace on the hub.
  # =========================================================================
  - name: policy-tenant-managed-rbac
    consolidateManifests: false
    manifests:
      - path: rbac/managed-rolebindings.yaml

policySets:
  - name: tenancy-managed-access-control
    description: Managed cluster tenant RoleBindings for namespace access
    placement:
      placementName: placement-managed-clusters
